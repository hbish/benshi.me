---
phase: 01-cloudflare-setup
plan: 01
type: execute
---

<objective>
Create Cloudflare Pages project and connect to GitHub repository.

Purpose: Establish the Cloudflare Pages project foundation with Git integration for automatic
deployments. Output: Cloudflare Pages project created and connected to the benshi.me GitHub
repository. </objective>

<execution_context> ~/.claude/get-shit-done/workflows/execute-phase.md ./summary.md
~/.claude/get-shit-done/references/checkpoints.md </execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md

**Tech stack available:**

- Astro 5.1.3 static site generator
- pnpm package manager
- Node.js 22.12.0
- Build output: dist/

**Established patterns:**

- Standard Astro build: `pnpm build` outputs to `dist/`
- No environment variables required
- Static site with no server-side dependencies

**Key files:**

- astro.config.mjs - Astro configuration
- package.json - Build scripts and dependencies </context>

<tasks>

<task type="auto">
  <name>Task 1: Authenticate with Cloudflare (wrangler CLI)</name>
  <files>None (authentication only)</files>
  <action>Run `wrangler login` to authenticate with Cloudflare. This opens a browser for OAuth authentication. Do NOT proceed if authentication fails - this is a prerequisite for creating Pages projects.</action>
  <verify>`wrangler whoami` returns your Cloudflare account information</verify>
  <done>Cloudflare authentication active, wrangler CLI can access account</done>
</task>

<task type="auto">
  <name>Task 2: Create Cloudflare Pages project connected to GitHub</name>
  <files>None (Cloudflare console configuration)</files>
  <action>Create a new Cloudflare Pages project using wrangler CLI:
1. Run `wrangler pages project create benshi-me --production-branch=main`
2. Note the project ID for future reference
3. The project will be created but not yet configured with build settings (that's plan 01-02)

GitHub connection will be configured via Cloudflare dashboard after project creation, or via CLI if
supported. Use the project name "benshi-me" (with hyphen, as Cloudflare project names may differ
from domain).</action> <verify>`wrangler pages project list` includes "benshi-me" project</verify>
<done>Cloudflare Pages project "benshi-me" exists and is ready for build configuration</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `wrangler whoami` shows authenticated account
- [ ] `wrangler pages project list` shows "benshi-me" project
- [ ] Project is accessible in Cloudflare dashboard at https://dash.cloudflare.com
</verification>

<success_criteria>

- Cloudflare Pages project "benshi-me" created
- wrangler CLI authenticated and functional
- Ready for build configuration in plan 01-02 </success_criteria>

<output>
After completion, create `.planning/phases/01-cloudflare-setup/01-01-SUMMARY.md`:

# Phase 1 Plan 1: Create Cloudflare Pages Project Summary

**Cloudflare Pages project created and connected to GitHub repository.**

## Accomplishments

- Authenticated with Cloudflare using wrangler CLI
- Created Cloudflare Pages project "benshi-me"
- Project ID: [ID from wrangler output]

## Files Created/Modified

- None (console/CLI operations only)

## Decisions Made

- Project name: "benshi-me" (hyphenated for Cloudflare compatibility)
- Production branch: main (matches current git branch)

## Issues Encountered

None

## Next Step

Ready for 01-02-PLAN.md - Configure build settings (pnpm build, Node.js 22, dist/ output) </output>
