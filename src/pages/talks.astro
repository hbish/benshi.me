---
import { getEntry } from 'astro:content'
import Page from '../layouts/Page.astro'

const page = await getEntry('pages', 'talks')
if (!page) throw new Error('Talks page not found')
---

<Page page={page} />
