---
import { getEntry } from 'astro:content';
import Page from '../layouts/Page.astro';

const page = await getEntry('pages', 'versions');
if (!page) throw new Error('Versions page not found');
---

<Page page={page} />